# ============================================================================
# Frontend Environment Configuration
# ============================================================================
#
# API Configuration
# -----------------
# These values should be updated when transitioning from IP to domain name.
# For GitHub Actions deployment, update the corresponding secrets.
#
# Development: Use localhost (http://localhost:5000/api/v1)
# Staging: Use IP address (http://18.61.71.16:5000/api/v1)
# Production: Use domain (https://api.yourdomain.com/api/v1)
# ============================================================================

# API Base URL - Full URL for API endpoints
# For local development, use: http://localhost:5000/api/v1
VITE_API_BASE_URL=http://localhost:5001/api/v1

# Backend URL - Base URL for the backend server (for static assets, uploads)
# This should be the same host as API_BASE_URL but without the /api/v1 path
# For local development, use: http://localhost:5000
VITE_BACKEND_URL=http://localhost:5001
