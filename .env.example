# ============================================================================
# Frontend Environment Configuration - Example Template
# ============================================================================
# 
# Copy this file to .env and update the values for your environment.
# DO NOT commit your .env file to version control!
#
# For GitHub Actions deployment:
# - Set these as repository secrets
# - The build process will inject them during the build
#
# ============================================================================

# ============================================================================
# API Configuration
# ============================================================================

# API Base URL - Full URL for API endpoints
# 
# Examples:
#   - Local development: http://localhost:5000/api/v1
#   - Staging (IP):      http://18.61.71.16:5000/api/v1
#   - Production:        https://api.yourdomain.com/api/v1
#
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Backend URL - Base URL for static assets (uploads, images)
#
# This should be the same host as VITE_API_BASE_URL but without /api/v1
#
# Examples:
#   - Local development: http://localhost:5000
#   - Staging (IP):      http://18.61.71.16:5000
#   - Production:        https://api.yourdomain.com
#
VITE_BACKEND_URL=http://localhost:5000

# ============================================================================
# Optional Configuration
# ============================================================================

# App name displayed in the UI
# VITE_APP_NAME=My Group

# App version for display purposes
# VITE_APP_VERSION=1.0.0

# ============================================================================
# Notes for Production Deployment
# ============================================================================
#
# SAME-ORIGIN DEPLOYMENT (Recommended for Docker):
# When frontend is served from the same server as the backend, use relative URLs:
#
#   VITE_API_BASE_URL=/api/v1
#   VITE_BACKEND_URL=
#
# This avoids CSP issues and works automatically with any domain or IP!
# The Dockerfile uses these defaults automatically.
#
# DIFFERENT-ORIGIN DEPLOYMENT:
# If frontend and backend are on different servers:
#
#   VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
#   VITE_BACKEND_URL=https://api.yourdomain.com
#
# When transitioning from IP address to domain name:
#
# 1. Update GitHub Actions secrets:
#    - VITE_API_BASE_URL (optional - defaults work for same-origin)
#    - VITE_BACKEND_URL (optional - defaults work for same-origin)
#
# 2. For the backend, also update:
#    - FRONTEND_URL (for CORS configuration)
#    - CORS_ORIGIN (allowed origins)
#
# No code changes are required - just environment configuration updates!
#
# ============================================================================

