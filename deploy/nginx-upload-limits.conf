# Include inside the server { } block for gomygroup.online (or your API vhost).
# Required for media-document uploads up to 200MB.
client_max_body_size 200m;
proxy_read_timeout 600s;
proxy_send_timeout 600s;
proxy_request_buffering off;
